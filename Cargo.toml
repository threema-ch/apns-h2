[package]
name = "apns-h2"
version = "0.11.0-alpha.3"
edition = "2024"
rust-version = "1.85"
description = "Async HTTP/2 APNs client based on hyper"
documentation = "https://docs.rs/apns-h2"
readme = "README.md"
homepage = "https://threema.ch/"
repository = "https://github.com/threema-ch/apns-h2.git"
license = "MIT"
keywords = ["apns", "apple", "async", "http2", "push"]

[dependencies]
aws-lc-rs = { version = "1.15.4", optional = true }
base64 = "0.22"
erased-serde = "0.4"
http = "1.0"
http-body-util = "0.1"
hyper = { version = "1.0", default-features = false, features = [
    "client",
    "http2",
] }
hyper-rustls = { version = "0.27", default-features = false, features = [
    "http2",
    "native-tokio",
    "rustls-platform-verifier",
] }
hyper-util = { version = "0.1", features = [
    "client",
    "client-legacy",
    "http2",
    "tokio",
] }
openssl = { version = "0.10", optional = true }
p12-keystore = { version = "0.2.0", optional = true }
parking_lot = "0.12"
pem = { version = "3.0", optional = true }
rustls = { version = "0.23", default-features = false, features = [
    "logging",
    "std",
    "tls12",
] }
rustls-openssl = { version = "0.3.0", optional = true }
rustls-pki-types = { version = "1.14.0", features = ["std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["time"] }
tracing = { version = "0.1", optional = true }

[dev-dependencies]
argparse = "0.2"
p256 = { version = "0.13.2", default-features = false, features = ["ecdsa", "std", "pem"] }
pkcs8 = "0.10.2"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tracing-subscriber = "0.3"

[features]
default = ["aws-lc-rs"]
openssl = ["dep:openssl", "rustls-openssl"]
aws-lc-rs = ["dep:aws-lc-rs", "hyper-rustls/aws-lc-rs", "p12-keystore", "pem", "rustls/aws-lc-rs"]
tracing = ["dep:tracing", "tokio/tracing"]
